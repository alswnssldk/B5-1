# SQL 데이터베이스 설계 및 쿼리 실습 미션

## 1. 미션 개요

이번 미션은 백엔드 프레임워크 없이 SQL만 사용하여 직접 데이터베이스를 설계하고, 샘플 데이터를 입력한 뒤, 실제 요구사항을 SQL 쿼리로 해결하는 것을 목표로 한다.

데이터베이스가 엑셀과 다른 점은 단순히 데이터를 많이 저장하는 것이 아니라, 여러 테이블 사이의 관계를 PK와 FK로 연결하고 데이터의 무결성을 지킬 수 있다는 점이다.

본 프로젝트에서는 `[주제명 입력]` 도메인을 기준으로 최소 4개 이상의 테이블을 설계하고, 1:N 관계, 제약조건, JOIN, GROUP BY, 서브쿼리, UPDATE, DELETE, INDEX를 직접 작성하였다.

---

## 2. 개발 환경

| 항목 | 내용 |
|---|---|
| 사용 DB | `[SQLite / MySQL / PostgreSQL / H2 중 입력]` |
| SQL 실행 도구 | `[DBeaver / DataGrip / TablePlus / CLI 등 입력]` |
| 운영체제 | `[Windows / macOS / Linux 입력]` |
| 제출 파일 형식 | `.sql`, `.md`, 이미지 또는 텍스트 결과 파일 |

---

## 3. 제출물 구성

```text
db-mission/
├── 01_schema.sql
├── 02_insert_data.sql
├── 03_queries.sql
├── README.md
├── results/
│   ├── q01.png
│   ├── q02.png
│   ├── q03.png
│   ├── ...
│   └── q15.png
└── images/
    ├── erd.png
    └── fk_error.png
```

| 파일 / 폴더 | 설명 |
|---|---|
| `01_schema.sql` | 테이블 생성, PK, FK, 제약조건 정의 |
| `02_insert_data.sql` | 각 테이블별 샘플 데이터 INSERT |
| `03_queries.sql` | 핵심 SQL 쿼리 15개 작성 |
| `results/` | 각 쿼리 실행 결과 캡처 또는 텍스트 |
| `images/erd.png` | ERD 다이어그램 이미지 |
| `images/fk_error.png` | FK 제약조건 테스트 결과 이미지 |

---

## 4. 도메인 소개

### 4.1 주제

`[예: 카페 주문 관리 시스템 / 도서 대여 시스템 / 영화 평점 시스템 등]`

### 4.2 주제 선정 이유

`[이 주제를 선택한 이유 작성]`

예시:

> 카페 주문 관리 시스템은 고객, 메뉴, 주문, 주문 상세처럼 자연스럽게 여러 테이블로 나누기 좋고, 고객 1명이 여러 주문을 할 수 있으며 주문 1건에 여러 메뉴가 포함될 수 있어 1:N 관계를 이해하기에 적합하다고 판단하였다.

---

## 5. ERD 다이어그램

아래 이미지는 본 프로젝트의 테이블 구조와 관계를 나타낸 ERD이다.

![ERD](./images/erd.png)

---

## 6. 테이블 설계

### 6.1 전체 테이블 목록

| 테이블명 | 역할 |
|---|---|
| `[table_1]` | `[역할 작성]` |
| `[table_2]` | `[역할 작성]` |
| `[table_3]` | `[역할 작성]` |
| `[table_4]` | `[역할 작성]` |
| `[table_5]` | `[필요 시 작성]` |

---

### 6.2 테이블별 컬럼 설명

#### `[table_1]`

| 컬럼명 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `[id]` | `INTEGER` | `PRIMARY KEY` | 기본키 |
| `[column]` | `TEXT` | `NOT NULL` | `[설명]` |
| `[column]` | `TEXT` | `UNIQUE` | `[설명]` |

#### `[table_2]`

| 컬럼명 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `[id]` | `INTEGER` | `PRIMARY KEY` | 기본키 |
| `[column]` | `TEXT` | `NOT NULL` | `[설명]` |

#### `[table_3]`

| 컬럼명 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `[id]` | `INTEGER` | `PRIMARY KEY` | 기본키 |
| `[table_1_id]` | `INTEGER` | `FOREIGN KEY` | `[table_1]` 테이블 참조 |
| `[column]` | `DATE` | `NOT NULL` | `[설명]` |

#### `[table_4]`

| 컬럼명 | 타입 | 제약조건 | 설명 |
|---|---|---|---|
| `[id]` | `INTEGER` | `PRIMARY KEY` | 기본키 |
| `[table_3_id]` | `INTEGER` | `FOREIGN KEY` | `[table_3]` 테이블 참조 |
| `[column]` | `INTEGER` | `NOT NULL` | `[설명]` |

---

## 7. 테이블 관계 설명

### 7.1 1:N 관계 목록

| 부모 테이블 | 자식 테이블 | 관계 | 설명 |
|---|---|---|---|
| `[부모 테이블]` | `[자식 테이블]` | `1:N` | `[예: 고객 1명은 여러 주문을 가질 수 있다.]` |
| `[부모 테이블]` | `[자식 테이블]` | `1:N` | `[예: 주문 1건은 여러 주문 상세를 가질 수 있다.]` |
| `[부모 테이블]` | `[자식 테이블]` | `1:N` | `[필요 시 작성]` |

### 7.2 PK와 FK 설명

본 프로젝트에서 PK는 각 테이블의 데이터를 고유하게 식별하기 위해 사용하였다.

예시:

```sql
PRIMARY KEY (id)
```

FK는 테이블 사이의 관계를 연결하고, 존재하지 않는 데이터를 참조하지 못하도록 막기 위해 사용하였다.

예시:

```sql
FOREIGN KEY (customer_id) REFERENCES customer(customer_id)
```

---

## 8. 제약조건 적용 내용

| 제약조건 | 적용 위치 | 적용 이유 |
|---|---|---|
| `PRIMARY KEY` | 모든 테이블의 ID 컬럼 | 각 행을 고유하게 식별하기 위해 사용 |
| `FOREIGN KEY` | `[자식 테이블.컬럼명]` | 부모 테이블에 존재하는 데이터만 참조하도록 하기 위해 사용 |
| `NOT NULL` | `[테이블명.컬럼명]` | 반드시 입력되어야 하는 값이기 때문에 사용 |
| `UNIQUE` | `[테이블명.컬럼명]` | 중복되면 안 되는 값이기 때문에 사용 |

---

## 9. 샘플 데이터 입력

각 테이블에는 최소 10행 이상의 샘플 데이터를 입력하였다.

| 테이블명 | 데이터 수 |
|---|---:|
| `[table_1]` | `10행 이상` |
| `[table_2]` | `10행 이상` |
| `[table_3]` | `10행 이상` |
| `[table_4]` | `10행 이상` |
| `[table_5]` | `10행 이상` |

데이터 입력 시 FK 관계를 만족시키기 위해 부모 테이블 데이터를 먼저 입력한 후, 자식 테이블 데이터를 입력하였다.

입력 순서:

```text
1. [부모 테이블 1]
2. [부모 테이블 2]
3. [자식 테이블 1]
4. [자식 테이블 2]
5. [자식 테이블 3]
```

---

## 10. 핵심 SQL 쿼리 15개

아래 쿼리들은 `03_queries.sql` 파일에 작성하였다.

### 10.1 기본 조회 쿼리

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q01 | `[전체 데이터 조회]` | `SELECT` | `results/q01.png` |
| Q02 | `[조건에 맞는 데이터 조회]` | `WHERE` | `results/q02.png` |
| Q03 | `[정렬 조회]` | `ORDER BY` | `results/q03.png` |
| Q04 | `[일부 데이터 제한 조회]` | `LIMIT` | `results/q04.png` |

### 10.2 JOIN 쿼리

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q05 | `[INNER JOIN 예시 1]` | `INNER JOIN` | `results/q05.png` |
| Q06 | `[INNER JOIN 예시 2]` | `INNER JOIN` | `results/q06.png` |
| Q07 | `[여러 테이블 JOIN]` | `JOIN` | `results/q07.png` |
| Q08 | `[LEFT JOIN 예시]` | `LEFT JOIN` | `results/q08.png` |

### 10.3 집계 쿼리

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q09 | `[개수 집계]` | `COUNT`, `GROUP BY` | `results/q09.png` |
| Q10 | `[합계 집계]` | `SUM`, `GROUP BY` | `results/q10.png` |
| Q11 | `[평균 집계]` | `AVG`, `GROUP BY` | `results/q11.png` |

### 10.4 서브쿼리

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q12 | `[서브쿼리 사용 예시]` | `SUBQUERY` | `results/q12.png` |

### 10.5 데이터 수정 및 삭제

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q13 | `[데이터 수정]` | `UPDATE` | `results/q13.png` |
| Q14 | `[데이터 삭제]` | `DELETE` | `results/q14.png` |

### 10.6 인덱스

| 번호 | 쿼리 설명 | 사용 문법 | 결과 파일 |
|---|---|---|---|
| Q15 | `[인덱스 생성]` | `CREATE INDEX` | `results/q15.png` |

인덱스 적용 이유:

> `[예: 주문 날짜 기준으로 검색하는 경우가 많기 때문에 orders.ordered_at 컬럼에 인덱스를 생성하였다.]`

---

## 11. 쿼리 실행 결과

각 쿼리 실행 결과는 `results/` 폴더에 저장하였다.

예시:

```text
results/
├── q01.png
├── q02.png
├── q03.png
├── q04.png
├── q05.png
├── q06.png
├── q07.png
├── q08.png
├── q09.png
├── q10.png
├── q11.png
├── q12.png
├── q13.png
├── q14.png
└── q15.png
```

---

## 12. 주요 쿼리 상세 설명

가장 복잡했던 쿼리:

```sql
-- 여기에 가장 복잡했던 쿼리 작성
```

### 쿼리 목적

`[이 쿼리로 무엇을 확인하려고 했는지 작성]`

예시:

> 메뉴별 총 판매 수량과 총 매출을 확인하기 위한 쿼리이다.

### 동작 과정

1. `[첫 번째 테이블]`에서 기본 데이터를 가져온다.
2. `[두 번째 테이블]`과 JOIN하여 관련 데이터를 연결한다.
3. `[기준 컬럼]`을 기준으로 GROUP BY를 수행한다.
4. `COUNT / SUM / AVG` 함수를 사용하여 집계 결과를 계산한다.
5. 필요한 경우 `ORDER BY`로 결과를 정렬한다.

### 실행 결과

![주요 쿼리 결과](./results/q복잡한쿼리번호.png)

---

## 13. INNER JOIN과 LEFT JOIN 차이

### INNER JOIN

`INNER JOIN`은 양쪽 테이블에 모두 연결되는 데이터만 조회한다.

예시:

```sql
-- INNER JOIN 예시 작성
```

설명:

> `[예: 주문 정보와 고객 정보를 INNER JOIN하면 실제 주문이 존재하고, 해당 주문과 연결된 고객 정보가 있는 데이터만 조회된다.]`

### LEFT JOIN

`LEFT JOIN`은 왼쪽 테이블의 데이터는 모두 보여주고, 오른쪽 테이블에 연결되는 데이터가 없으면 NULL로 표시한다.

예시:

```sql
-- LEFT JOIN 예시 작성
```

설명:

> `[예: 고객 테이블을 기준으로 주문 테이블을 LEFT JOIN하면 주문한 적 없는 고객도 함께 조회할 수 있다.]`

---

## 14. GROUP BY와 집계 함수 설명

`GROUP BY`는 특정 컬럼을 기준으로 데이터를 묶고, `COUNT`, `SUM`, `AVG` 같은 집계 함수를 사용하여 통계 결과를 구할 때 사용한다.

| 함수 | 의미 | 사용 예시 |
|---|---|---|
| `COUNT` | 개수 계산 | 고객별 주문 횟수 |
| `SUM` | 합계 계산 | 메뉴별 총 판매 수량 |
| `AVG` | 평균 계산 | 카테고리별 평균 가격 |

---

## 15. FK 제약조건 테스트

존재하지 않는 부모 데이터를 참조하는 INSERT를 시도하여 FK 제약조건이 실제로 동작하는지 확인하였다.

### 테스트 쿼리

```sql
-- FK 에러 테스트 쿼리 작성
-- 예시:
-- INSERT INTO orders (order_id, customer_id, ordered_at, status)
-- VALUES (999, 9999, '2026-05-27', 'ORDERED');
```

### 결과

![FK Error](./images/fk_error.png)

### 설명

> 존재하지 않는 부모 테이블의 ID를 참조하려고 했기 때문에 FK 제약조건에 의해 INSERT가 실패하였다. 이를 통해 데이터베이스가 잘못된 관계의 데이터 입력을 막고, 데이터 무결성을 지킨다는 것을 확인할 수 있었다.

---

## 16. 엑셀과 데이터베이스의 차이

엑셀은 하나의 표 안에 데이터를 직접 입력하고 관리하는 방식에 가깝다. 반면 데이터베이스는 데이터를 여러 테이블로 나누어 저장하고, PK와 FK를 통해 테이블 사이의 관계를 명확하게 표현할 수 있다.

또한 데이터베이스는 제약조건을 통해 잘못된 데이터 입력을 막을 수 있고, JOIN과 GROUP BY를 사용하여 여러 테이블의 데이터를 연결하거나 집계할 수 있다.

---

## 17. 어려웠던 점과 해결 방법

### 어려웠던 점

`[미션 수행 중 어려웠던 부분 작성]`

예시:

> 처음에는 주문 테이블 하나에 고객 정보와 메뉴 정보를 모두 넣으려고 했지만, 그렇게 하면 같은 고객 정보와 메뉴 정보가 반복되어 데이터 중복이 발생한다는 문제가 있었다.

### 해결 방법

`[어떻게 해결했는지 작성]`

예시:

> 고객, 메뉴, 주문, 주문 상세 테이블로 나누고, PK와 FK를 사용하여 각 데이터를 연결하는 방식으로 해결하였다. 이를 통해 데이터 중복을 줄이고, JOIN을 사용하여 필요한 정보를 다시 조회할 수 있었다.

---

## 18. 보너스 과제 수행 내용

### 18.1 JOIN과 서브쿼리 비교

같은 요구사항을 JOIN과 서브쿼리 두 가지 방식으로 작성하였다.

#### JOIN 방식

```sql
-- JOIN 방식 쿼리 작성
```

#### 서브쿼리 방식

```sql
-- 서브쿼리 방식 쿼리 작성
```

#### 차이점

> `[JOIN과 서브쿼리의 차이점 작성]`

---

### 18.2 FK 오류 발생 및 해결

존재하지 않는 FK 값을 입력했을 때 오류가 발생하는 것을 확인하였다.

```sql
-- FK 오류 발생 쿼리 작성
```

해결 방법:

> `[올바른 부모 데이터를 먼저 INSERT하거나, 존재하는 부모 ID를 참조하도록 수정하였다.]`

---

### 18.3 미니 리포트

본 데이터베이스를 통해 확인할 수 있는 핵심 지표 3가지는 다음과 같다.

| 지표 | 설명 | 관련 쿼리 |
|---|---|---|
| `[지표 1]` | `[설명]` | `[Q번호]` |
| `[지표 2]` | `[설명]` | `[Q번호]` |
| `[지표 3]` | `[설명]` | `[Q번호]` |

---

## 19. 평가항목 체크리스트

### 항목 1. 결과물 완성 여부

- [ ] 최소 4개 이상의 테이블을 생성하였다.
- [ ] 각 테이블에 PK를 정의하였다.
- [ ] FK를 사용한 1:N 관계가 최소 2개 이상 존재한다.
- [ ] 각 테이블에 최소 10행 이상의 샘플 데이터를 입력하였다.
- [ ] 기본 조회 4개, 조인 4개, 집계 3개, 서브쿼리 1개, 수정/삭제 2개, 인덱스 1개를 포함하여 총 15개 이상의 쿼리를 작성하였다.
- [ ] 각 쿼리의 실행 결과를 스크린샷 또는 텍스트로 첨부하였다.

### 항목 2. 설계 설명 가능 여부

- [ ] 테이블을 왜 나누었는지 설명할 수 있다.
- [ ] 각 테이블의 역할을 설명할 수 있다.
- [ ] FK로 연결된 1:N 관계가 실제 도메인에서 어떤 의미인지 설명할 수 있다.
- [ ] 컬럼 타입을 왜 그렇게 선택했는지 설명할 수 있다.
- [ ] 인덱스를 어떤 컬럼에 걸었고, 왜 필요한지 설명할 수 있다.

### 항목 3. DB 개념 이해 여부

- [ ] 데이터베이스와 엑셀의 차이를 설명할 수 있다.
- [ ] PK와 FK의 역할을 구분하여 설명할 수 있다.
- [ ] 1:N 관계가 데이터를 어떻게 연결하는지 설명할 수 있다.
- [ ] INNER JOIN과 LEFT JOIN의 차이를 실행 결과를 통해 설명할 수 있다.
- [ ] GROUP BY와 COUNT, SUM, AVG의 동작 방식을 설명할 수 있다.

### 항목 4. 문제 해결 과정 설명 여부

- [ ] 작성한 쿼리 중 가장 복잡한 쿼리를 선택하였다.
- [ ] 해당 쿼리를 어떻게 작성했는지 단계별로 설명할 수 있다.
- [ ] 미션 수행 중 어려웠던 부분을 정리하였다.
- [ ] 어려웠던 부분을 어떻게 해결했는지 구체적으로 작성하였다.

### 항목 5. 보너스 과제

- [ ] JOIN과 서브쿼리 두 방식으로 같은 요구사항을 해결하였다.
- [ ] FK 오류를 일부러 발생시키고, 왜 막히는지 기록하였다.
- [ ] 핵심 지표 3개를 정의하고 SQL로 작성하였다.

---

## 20. 최종 정리

이번 미션을 통해 테이블을 나누는 이유, PK와 FK를 통한 관계 설정, JOIN을 통한 데이터 연결, GROUP BY를 통한 집계 방식, 그리고 인덱스의 필요성을 학습하였다.

단순히 SQL 문법을 작성하는 것에서 끝나는 것이 아니라, 실제 도메인을 기준으로 데이터를 어떻게 나누고 연결해야 하는지 이해하는 것이 핵심이었다.
